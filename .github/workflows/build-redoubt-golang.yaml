name: build-redoubt-golang

on:
  push:
    branches: [main]
    paths:
      - "images/golang/**"
      - ".github/workflows/build-redoubt-golang.yaml"
      - ".github/workflows/ci.yaml"
    tags:
      - "golang/v*"
  pull_request:
    paths:
      - "images/golang/**"
      - ".github/workflows/build-redoubt-golang.yaml"
      - ".github/workflows/ci.yaml"
  workflow_dispatch:

jobs:
  golang-build:
    uses: ./.github/workflows/ci.yaml
    with:
      image-name: golang-build
      dockerfile: Dockerfile.builder
      context: images/golang
    permissions:
      contents: read
      packages: write
      id-token: write
      security-events: write
  golang:
    uses: ./.github/workflows/ci.yaml
    with:
      image-name: golang
      dockerfile: Dockerfile.runtime
      context: images/golang
    permissions:
      contents: read
      packages: write
      id-token: write
      security-events: write
